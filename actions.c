#include "cleanup.h"

// 나중에 B파트가 구현할 함수들 (지금은 비워둠)
void move_file(FileInfo *file, const char *dest_folder) {
    // 텅 빔
}

void archive_files(FileInfo *head, const char *archive_name) {
    // 텅 빔
}

int check_duplicate(FileInfo *file1, FileInfo *file2) {
    return 0;
}